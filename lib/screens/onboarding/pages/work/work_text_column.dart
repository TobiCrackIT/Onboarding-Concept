import 'package:flutter/material.dart';
import 'package:onboarding/screens/onboarding/widgets/text_column.dart';

class WorkTextColumn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return TextColumn(
      title: 'Work together',
      text:
      'Work can be more interesting than you know it now. We are creating customised work experiences for everyone.',
    );
  }
}